# 未決事項と決定ログ

**未決事項は Claude Code が独断で決めないこと。** 判断が必要になったらユーザーに確認し、
決定したら決定ログに追記のうえ、該当する文書(requirements / architecture)に反映する。

## 未決事項

| # | 項目 | 現在の暫定 | 判断材料 |
|---|---|---|---|
| 7 | アプリの常駐(タスクトレイ)を許容するか | v0.4 のスケジューラは**ウィンドウ起動中のみ**動作(UI に明記)。常駐は未実装 | 常駐すればウィンドウを閉じても定期実行が動く。roadmap v0.4 参照 |
| 8 | 更新・定義同期の配布元 | 共有フォルダ方式で実装済み(update_source / sharedAgentsSource)。GitHub 方式は保留 | private リポジトリの PAT 配布問題(roadmap v0.3) |
| 3 | モデルの指定方法(エージェント定義側か、アプリ設定側か) | **暫定運用で実装済み**: エージェント定義の `model` > アプリの `defaultModel` > SDK 既定(SDK の CustomAgentConfig の仕様上この優先順が自然なため)。正式決定は保留 | 両方技術的に可能。競合時の優先順位は公式に記載なし |
| 4 | 実行中のセッション数の上限 | **暫定運用で実装済み(v0.5)**: config.json の `maxConcurrentTasks`(既定 2)。正式な上限値は運用を見て決定 | Copilot のレート・クレジット消費は並行数に比例(roadmap: 小さく始める) |
| 5 | ログの保持期間 | **暫定運用で実装(v0.6)**: config.json の `logRetentionDays`(既定 90、0=無制限)。起動時に data/logs/ の古いファイルを削除。history.jsonl は対象外(小さいため無期限)。正式値は運用を見て決定 | 無制限だと data/logs/ が肥大する |
| 9 | 依頼フォーム/テンプレート変数の置き場と記法 | 未着手(roadmap v1.1) | `.agent.md` frontmatter の独自キーなら共有フォルダ同期でそのまま配れる(CLI 単体では無視される)が、未知キーを保存で落とさない修正が前提。agents.json 側なら定義と設定が分かれる |
| 10 | MCP サーバーの利用を許容するか | 未着手(roadmap v1.3)。現状 `mcp-servers` はパース対象外で、定義エディタ保存時に失われる | 入力データの自動取得に有効だが、MCP 経由の操作は outputDir 判定の外側になりガードレールの範囲が広がる。接続情報の置き場は「認証情報を保存しない」方針([requirements.md](requirements.md) §2.3)と要整合 |

(#1 名称、#2 フロントエンド、#6 CLI ポリシーは解決済み → 決定ログ)

## 決定ログ

- 2026-08-12: v0.2 の同期方式の暫定決定(ユーザー未確認): まず共有フォルダ同期のみ実装
  (認証不要・ローカル検証可能)。GitHub リポジトリ同期は配布元と認証(PAT)が未決のため保留。
  個人スコープ = agentDirs(編集可)、共有スコープ = data/shared-agents(読み取り専用)、同名は個人優先

- 2026-08-12: #3 の暫定運用を決定(ユーザー未確認): モデルはエージェント定義 > アプリ defaultModel > SDK 既定。ステップ3実装に反映、コードに「暫定」明記

- 2026-08-12: #1 正式名称を `agent-deck` に決定(ユーザー決定)
- 2026-08-12: #2 フロントエンドは推奨どおり React + TypeScript + Vite で確定(実装済み)
- 2026-08-12: #6 組織の Copilot CLI ポリシーは有効と確認。CLI 1.0.79(winget)で認証・プロンプト疎通 OK
- 2026-08-12: Git リポジトリをユーザーが初期化
- 2026-08-12: 初期仕様書(agent-deck-spec.md)を docs/ 配下の文書群に分割・再編。原本は docs/archive/ に保存
